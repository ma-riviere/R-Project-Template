```{r include = F, echo = F}
if(!"here" %in% utils::installed.packages()) {install.packages("here"); library(here)}

source(here::here("src", "common", "init.R"), echo = F)

setup_project(install = F, update = F, clean = F)
```
